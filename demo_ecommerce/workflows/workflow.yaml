# E-Commerce Order Flow Workflow
steps:
  # 1. Create Order
  - process: "create_order"
  
  # 2. Process Payment
  - process: "process_payment"
  
  # 3. Store Invoice (Heavy Zone)
  - process: "store_invoice_image"
